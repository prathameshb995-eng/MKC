#include <stdio.h>
#include <conio.h>
int fact(int n);
void main()
{
int n, result;
clrscr();
printf("Enter a number: ");
scanf("%d", &n);
result = fact(n);
printf("Factorial of %d is %d", n, result);
getch();
}
int fact(int n)
{
if(n == 0)
return 1;
else
return n * fact(n - 1);
}
